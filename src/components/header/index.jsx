import { Typography, Container } from "@mui/material";

function Header() {
  return (
    <Container>
      <Typography variant="h1">Среда</Typography>
      <Typography variant="h2">3 января</Typography>
    </Container>
  );
}

export default Header;
